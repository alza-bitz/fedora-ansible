---
- hosts: localhost
  connection: local
  sudo: yes

  vars: 
    user: alex

  roles:
    - bash
    - ansible
    - ssh
    - docker
    - xrdp
    - { role: chromium, set_default_browser: true }
    - eclipse
    - bats

- hosts: localhost
  connection: local
  sudo: no

  vars:
    project_dir: ~/Projects

  roles:
    - drake
    - drip
