---
- hosts: localhost
  connection: local
  sudo: yes

  vars: 
    user: alex

  roles:
    - bash
    - ansible
    - ssh
    - docker
    - xrdp
    - chromium
    - eclipse
    - bats

- hosts: localhost
  connection: local
  sudo: no

  vars:
    project_dir: ~/Projects

  roles:
    - drake
    - drip
