---
- hosts: localhost
  connection: local
  sudo: yes

  vars: 
    user: alex

  roles:
    - ansible
    - ssh
    - docker
    - chromium
    - eclipse
    - bats

- hosts: localhost
  connection: local
  sudo: no

  vars:
    user: alex

  roles:
    - drake
    - drip
